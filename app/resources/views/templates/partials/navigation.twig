<!-- As a link -->
<nav class="navbar navbar-bg justify-content-between">  
    <span class="navbar-brand mt-3 mb-3">
    {% if auth.check %}
      Welcome to the Thinking of You Charity Portal
    {% else %}
      Charity Login
    {% endif %}
    </span>
    {% if auth.check %}
      <a class="nav-link pos-right" href="{{ path_for('auth.signout') }}">Sign out</a>
    {% endif %}  
</nav>